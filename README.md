# Langgraph_claimprocessor
It is a lightweight insurance claim evaluation app built with Streamlit &amp; LangGraph. It takes a claim data -> evaluate it against claim history in ChromaDB, evaluates the risk level with an LLM, validates the claim completeness, and routes the result to approval, rejection, or manual review. It shows chromaDB data and transversal of the Langgraph
